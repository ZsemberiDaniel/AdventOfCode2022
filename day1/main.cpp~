#include <fstream>
#include <vector>
#include <iostream>
#include <string>
#include <optional>

void readIntegerLines(const std::string& fileName, const std::vector<std::optional<int>>& out) {
    std::string output;
    std::ifstream istream;

    istream.open(fileName, std::ios::in);
    while (getline(istream, output)) {
        int number = std::stoi(output);
        std::cout << number << std::endl;
    }
}

int main() {
    std::cout << "Start" << std::endl;
    std::vector<std::optional<int>> ints;
    readIntegerLines("input.txt", ints);
    return 0;
}
